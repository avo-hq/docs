import { Plugin } from 'rollup';
import { BuildConfig } from '../config';
import { SFCAsyncStyleCompileOptions } from '@vue/compiler-sfc';
interface BuildCssOption {
    root: string;
    publicBase: string;
    assetsDir: string;
    minify?: BuildConfig['minify'];
    inlineLimit?: number;
    cssCodeSplit?: boolean;
    preprocessOptions?: SFCAsyncStyleCompileOptions['preprocessOptions'];
}
export declare const createBuildCssPlugin: ({ root, publicBase, assetsDir, minify, inlineLimit, cssCodeSplit, preprocessOptions }: BuildCssOption) => Plugin;
export {};
